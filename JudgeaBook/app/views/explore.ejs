<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="grid grid-cols-3 gap-3">
                <% book.reverse().forEach(function(cover){%>
                    <div class="h-fit w-[25vw] flex flex-col text-white bg-zinc-800 border-2 border-gray-500 p-2 gap-2 rounded-xl shadow-lg">
                        <div class=""><a href="/book/<%=cover.isbn%>"><%= cover.title %></a></div>
                        <div class=" "><%= cover.author %></div>
                        <div class=" "><%= cover.genre%></div>
                        <div class=""><%= cover.dscrptn%></div>
                    </div>
                    <%})
                %>
            </div>
</body>
</html>