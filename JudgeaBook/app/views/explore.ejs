<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body class="bg-zinc-900">
    <%- include('partials/navbar', { user: user }) %>
    <main class="p-4">
        <div class="grid grid-cols-6 gap-3">
            <% book.reverse().forEach(function(cover){%>
                <div
                    class="h-[20vh] flex flex-col text-white bg-zinc-800 border-2 border-gray-500 p-4 justify-between rounded-xl shadow-lg">
                    <div class=""><a href="/book/<%=cover.isbn%>">
                            <%= cover.title %>
                        </a></div>
                    <div class=" ">
                        <div>by:</div>
                        <%= cover.author %>
                    </div>
                </div>
                <%}) %>
        </div>
    </main>
</body>

</html>